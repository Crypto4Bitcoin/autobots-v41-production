"use client";

import React from "react";
import { CheckCircle, XCircle, Edit3, ExternalLink } from "lucide-react";
import { cn } from "@/lib/utils";

const mockReviewItems = [
  { 
    id: "1", 
    title: "Future of AI Agents", 
    type: "LinkedIn Post", 
    masterContent: "AI agents are no longer just tools, they are team members...", 
    quality: 8.5, 
    virality: 7.2 
  },
  { 
    id: "2", 
    title: "Ollama Guide", 
    type: "X Thread", 
    masterContent: "Local AI is the new frontier. Here is how to run Ollama...", 
    quality: 9.0, 
    virality: 8.8 
  },
];

export default function ReviewBoardPage() {
  return (
    <div className="space-y-8">
      <div>
        <h2 className="text-3xl font-bold tracking-tight">Review Board</h2>
        <p className="text-zinc-500 mt-1">Approve, edit, or reject content before it goes live.</p>
      </div>

      <div className="grid grid-cols-1 gap-6">
        {mockReviewItems.map((item) => (
          <div key={item.id} className="glass-panel p-6 ring-1 ring-white/5 bg-zinc-900/10">
            <div className="flex items-start justify-between mb-6">
              <div>
                <div className="flex items-center gap-3 mb-1">
                  <h3 className="text-xl font-bold">{item.title}</h3>
                  <span className="px-2 py-0.5 bg-purple-500/10 text-purple-400 text-[10px] font-bold uppercase rounded border border-purple-500/20">
                    {item.type}
                  </span>
                </div>
                <p className="text-sm text-zinc-500">Awaiting final approval for queue scheduling.</p>
              </div>
              
              <div className="flex items-center gap-4">
                <div className="text-right">
                  <p className="text-[10px] uppercase tracking-widest text-zinc-600 font-bold mb-1">Avg Score</p>
                  <p className="text-lg font-bold text-cyan-400">{((item.quality + item.virality) / 2).toFixed(1)}</p>
                </div>
                <div className="h-10 w-[1px] bg-zinc-800" />
                <button className="p-2.5 bg-zinc-900 border border-zinc-800 rounded-lg text-zinc-400 hover:text-white transition-colors">
                  <Edit3 className="w-5 h-5" />
                </button>
              </div>
            </div>

            <div className="bg-black/40 rounded-xl p-5 border border-zinc-900 mb-6 font-serif italic text-zinc-300 leading-relaxed line-clamp-3">
              "{item.masterContent}"
            </div>

            <div className="flex items-center justify-between">
              <div className="flex items-center gap-6">
                <div>
                  <p className="text-[9px] uppercase tracking-widest text-zinc-500 font-bold mb-1">Quality</p>
                  <div className="flex gap-1">
                    {[1, 2, 3, 4, 10].map((s, i) => (
                      <div key={i} className={cn("w-3 h-1 rounded-sm", i < 4 ? "bg-cyan-500" : "bg-zinc-800")} />
                    ))}
                  </div>
                </div>
                <div>
                  <p className="text-[9px] uppercase tracking-widest text-zinc-500 font-bold mb-1">Virality</p>
                  <div className="flex gap-1">
                    {[1, 2, 3, 4, 10].map((s, i) => (
                      <div key={i} className={cn("w-3 h-1 rounded-sm", i < 3 ? "bg-purple-500" : "bg-zinc-800")} />
                    ))}
                  </div>
                </div>
              </div>

              <div className="flex items-center gap-3">
                <button className="flex items-center gap-2 px-4 py-2 bg-zinc-900 border border-red-500/20 text-red-500 font-bold rounded-lg hover:bg-red-500/10 transition-all text-xs">
                  <XCircle className="w-4 h-4" />
                  REJECT
                </button>
                <button className="flex items-center gap-2 px-6 py-2 bg-green-500 hover:bg-green-400 text-black font-bold rounded-lg transition-all shadow-[0_0_15px_rgba(34,197,94,0.3)] text-xs">
                  <CheckCircle className="w-4 h-4" />
                  APPROVE
                </button>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}
